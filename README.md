# Дизайн-система со Storybook и всеми типами тестов

Учебный проект демонстрирует, как организовать дизайн-систему на React + TypeScript с использованием Vite и Storybook 10, а также настроить полный набор проверок качества:

- 💡 Библиотека компонентов: `Button`, `TextInput`, `Select`, `CustomCheckbox`.
- 📚 Storybook с документацией, интерактивными историями и аддоном a11y.
- ✅ Unit- и интеграционные тесты на Vitest + Testing Library.
- 🧪 Interaction-тесты через `play`-функции и Storybook test-runner.
- 🖼️ Визуальные регрессы на Playwright (скриншоты собранного Storybook).

## Быстрый старт

```bash
npm install
npm run dev           # локальная витрина Vite (демо-форма)
npm run storybook     # документация компонентов
```

Основная витрина живёт в `src/App.tsx` и показывает, как компоненты могут собираться в сценарий формы.

## Структура

```
src/
  components/        # исходники компонентов и unit-тесты
  stories/           # истории и документация Storybook
  setupTests.ts      # общая конфигурация Vitest
.storybook/          # конфиг Storybook (main.ts, preview.ts)
tests/visual/        # визуальные регрессы Playwright
```

## Тестирование

| Тип проверки          | Команда                         | Инструменты                                                |
| --------------------- | ------------------------------ | --------------------------------------------------------- |
| Unit / интеграционные | `npm run test:unit`            | Vitest, @testing-library/react                            |
| Interaction (stories) | `npm run test:stories`         | Storybook test-runner (Playwright под капотом)            |
| CI-режим stories      | `npm run test:stories:ci`      | Storybook test-runner без Watch                           |
| Визуальные регрессы   | `npm run build-storybook &&`<br>`npm run test:visual` | Playwright + скриншоты собранного Storybook |

> При обновлении эталонных скриншотов используйте `npm run test:visual:update`.

### Пайплайн качества

1. **Разработка**: создайте компонент, добавьте историю и unit-тест.
2. **Интерактив**: опишите сценарии через `play` либо `args` — test-runner прогонит их автоматически.
3. **Доступность**: проверяется аддоном a11y прямо в Storybook и в test-runner.
4. **Визуальная регрессия**: соберите статический Storybook и прогоните Playwright, чтобы сравнить скриншоты.

# storybook-test
